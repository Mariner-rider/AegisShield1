name: Deploy Staging

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4
      - name: Trigger staging deploy
        run: |
          echo "Starting staging deploy"
          # Replace with helm/kubectl/terraform command for your staging infra
          echo "Deploy completed"
